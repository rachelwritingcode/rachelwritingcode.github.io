# frozen_string_literal: true

source "https://rubygems.org"
gemspec

# gem "webrick", "~> 1.7"
gem "thin"

# Core standard libraries removed in modern Ruby versions (Ruby 3.4/4.0+)
gem "logger"
gem "csv"
gem "base64"
gem "bigdecimal"

# Required for local web-serving (if not already present)
gem "webrick"
